# SmartFarm UI

Shows the latest complete `sensor_snapshot` row from `smartfarm.sqlite3`.

## Run

```powershell
python -m rpi.ui.app --db smartfarm.sqlite3 --host 127.0.0.1 --port 8000
```

Open:

```text
http://127.0.0.1:8000
```
